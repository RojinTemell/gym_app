enum WidgetSizes {
  videoCardHeight(76),
  videoCardWidth(327),
  videoCardImageWidth(82),
  elevatedButtonHeight(50),
  genderButtonSize(140);

  final double value;

  const WidgetSizes(this.value);
}
